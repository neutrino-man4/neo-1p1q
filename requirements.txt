h5py==3.16.0
loguru==0.7.3
matplotlib>=3.9
numpy==2.5.2
omegaconf==2.3.1
python-dotenv>=1.0
PennyLane==0.45.1
PennyLane-Lightning==0.45.0
scikit-learn==1.9.0
scipy==1.18.1
tqdm==4.70.0
wandb==0.29.0

# torch is only used for DataLoader plumbing, never GPU compute -- the CPU
# build avoids conflicting with jax's own CUDA stack below.
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.14.0+cpu

# jax backend (default; see configs/README.md). Pinned to 0.10.2 -- a newer
# jax breaks PennyLane 0.45.1's interface. cuda12_local assumes a system
# CUDA 12.x toolkit already on LD_LIBRARY_PATH. If jax.devices() doesn't
# report a CudaDevice (silent CPU fallback), pip's own nvidia-*-cu12 wheels
# are shadowing the system CUDA install -- remove them and let those libs
# fall back to the system install instead:
#   pip uninstall -y nvidia-cusparse-cu12 nvidia-nvjitlink-cu12 \
#       nvidia-cusolver-cu12 nvidia-nccl-cu12 nvidia-nvshmem-cu12 \
#       nvidia-cuda-cupti-cu12 nvidia-cuda-nvcc-cu12 nvidia-cuda-cccl-cu12 \
#       nvidia-cufft-cu12
jax[cuda12_local]==0.10.2
optax==0.2.8
nvidia-cudnn-cu12==9.26.0.51
