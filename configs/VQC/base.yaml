seed: 'base'
base_dir: '/work/abal/qae_hep/'
train: true
wires: 4
num_layers: 1
ancilla: False
trash_qubits: 3
shots: 2500
# Binary classification: which JetClass sample dirs are signal (label 1) vs background (label 0),
# and how many jets of each class to read for train / validation / test. The loader does equal
# sampling per class and logs the final split.
signal: 'TTBar_'
background: 'ZJetsToNuNu'
n_signal: 1000
n_background: 1000
n_signal_val: 200
n_background_val: 200
n_signal_test: 2000
n_background_test: 2000
batch_size: 100
epochs: 25
improv: 0.01
backend: 'autograd'
device_name: 'lightning.kokkos'
lr: 0.05
lr_decay: false
save: true
flat: false
evictable: false
separate_ancilla: false
resume: false
desc: 'Training run'
norm_pt: false
substructure: false
patience: 2
use_reuploading: false
loss: 'MSE'
save_dir: '/work/abal/qae_hep/saved_models/'
data_dir: '/ceph/abal/JetClass/'
dump: '/ceph/abal/QML/JetClass/dumps/' # only for testing
load: false # only for testing
scheme: 'min' # only for testing
log_wandb: false # only for testing
load_best: false # only for testing
operations_per_qubit: 3 # VQC's RZ/RY/RX per wire per layer
aux_weights:
  scale_factor: 1.0
  bias: 0.1
circuit_type: 'normal'